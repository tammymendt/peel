app {
    paths {
        home = "/tmp"
        systems = ${app.paths.home}"/systems"
        jobs = ${app.paths.home}"/jobs"
        results = ${app.paths.home}"/results"
    }
}

system {
    default {
        user = ${user.name}
        group = ${user.name}
        config {
            masters = ["localhost"]
            slaves = ["localhost"]
        }
    }
}
